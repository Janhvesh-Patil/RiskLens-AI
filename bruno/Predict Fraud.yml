info:
  name: Predict Fraud
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/predict/845"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
