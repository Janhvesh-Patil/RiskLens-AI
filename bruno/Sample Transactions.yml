info:
  name: Sample Transactions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/sample-transactions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
