info:
  name: Predict Invalid Transaction
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/predict/999999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
