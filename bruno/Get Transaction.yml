info:
  name: Get Transaction
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/transaction/936"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
