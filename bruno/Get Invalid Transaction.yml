info:
  name: Get Invalid Transaction
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/transaction/999999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
